
ssh() {
    user="${1%%@*}"
    host="${1#*@}"
    echo "Connected to HPC facility..."
    docker compose exec -u $user -w /home/$user $host bash
}

wstart() {
    docker compose up -d
}

wstop() {
    docker compose down
}

wstatus() {
    docker compose ps
}
