
ssh() {
    user="${1%%@*}"
    host="${1#*@}"
    echo "Connected to HPC facility..."
    sudo docker compose exec -u $user -w /home/$user $host bash
}

wstart() {
    sudo docker compose up -d
}

wstop() {
    sudo docker compose down
}

wstatus() {
    sudo docker compose ps
}
